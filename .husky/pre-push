# The build guard mirrors ci.yml: there is nothing to bundle until `src/` lands.
# Delete the guard — not the build — once it does.
pnpm run typecheck && pnpm run test:changed && { [ ! -d src ] || pnpm run build; }
